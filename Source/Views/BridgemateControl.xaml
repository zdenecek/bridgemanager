<UserControl x:Class="BridgeManager.Source.Views.BridgemateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BridgeManager.Source.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <WrapPanel>
            <Button Height="30" Width="100" Margin="5" Content="Send to BWS" Command="{Binding SendCurrentSessionCommand}"/>
            <Button Height="30" Width="100" Margin="5"
                    Command="{Binding StartBCSCommand}" > Start BCS</Button>
            <Button Height="30" Width="100" Margin="5"
                    Command="{Binding RetrieveResultsCommand}">Retrieve results</Button>
        </WrapPanel>
    </Grid>
</UserControl>
