<UserControl x:Class="BridgeManager.Source.Views.TournamentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BridgeManager.Source.Views"
             mc:Ignorable="d" 
             xmlns:p ="clr-namespace:BridgeManager.Properties"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <p:Strings x:Key="Strings"></p:Strings>
    </UserControl.Resources>
    
    <Grid>
        <WrapPanel Orientation="Vertical">
            <Button Height="30" Width="100" Margin="5" Content="Open" 
                    Command="{Binding OpenCommand}"/>
            <Button Height="30" Width="100" Margin="5"
                    Command="{Binding NewCommand}" > New</Button>
            <Button Height="30" Width="100" Margin="5"
                    Command="{Binding SaveAsCommand}"
                    Content="{x:Static p:Strings.tournament_save_as}" />


        </WrapPanel>
    </Grid>
</UserControl>
